# Databricks App process definition.
# Keep the command minimal here because target-specific environment variables are managed in the bundle.

command:
  - streamlit
  - run
  - app.py
  - --server.port
  - "8000"
  - --server.address
  - 0.0.0.0
